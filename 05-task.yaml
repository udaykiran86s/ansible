- name: variable level ansible
  hosts: local
  connection: local
  vars:
    username: uday
    mobile: 929090
    email: uday@gmail.com
  tasks:
    vars:
      username: kiran
    - name: print the variable
      ansible.builtin.debug:
        msg: "username is {{ username}} mobile is {{ mobile}} email is {{ email }}"
